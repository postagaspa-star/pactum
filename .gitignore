# Android / Gradle
.gradle/
**/build/
local.properties
.kotlin/
captures/
*.iml

# Keystore: MAI nel repo (la chiave di firma è per sempre)
*.jks
*.keystore

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Dati locali
server/*.db
.env

# APK release: artefatti di build, consegnati fuori dal repo (server/apk/README.md
# spiega come). La cartella resta tracciata via .gitkeep e README.
server/apk/*.apk

# IDE / OS
.idea/
.vscode/
Thumbs.db
desktop.ini

# Chiave di firma: MAI nel repo, in nessuna forma
keystore.properties
**/keystore.properties

# Backup del registro: artefatti, non nel repo
server/backup/*.db
# Il pacchetto del programma per il computer: si ricostruisce con pc/prove/crea-pacchetto.ps1
server/apk/*.zip
